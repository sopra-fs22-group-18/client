import React from "react";


const SessionWait = props => {
    const session = props.session;


    return(
        <div>Placeholder for session wait screen with id {session.sessionId} and host {session.hostUsername}</div>
    )
}

export default SessionWait;